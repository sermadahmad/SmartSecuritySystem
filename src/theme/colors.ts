const lightTheme = {
  primary: '#000000',
  secondary: '#FFFFFF',
};

const darkTheme = {
  primary: '#FFFFFF',
  secondary: '#000000',
};

const colors = { light: lightTheme, dark: darkTheme };

export default colors;
